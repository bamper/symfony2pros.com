{% extends "ProtonTutorialBundle::layout.html.twig" %}

{% block title %}New Tutorial{% endblock %}

{% block content %}
<h1>Write a tutorial</h1>

<form action="{{ path('tutorial_create') }}" method="post" {{ form_enctype(form) }}>
    <div class="clearfix">
        {{ form_label(form.title) }}
        <div class="input">
            {{ form_errors(form.title) }}
            {{ form_widget(form.title, { 'attr': {'class': 'span10'}}) }}
        </div>
    </div>
    <div class="clearfix">
        {{ form_label(form.description) }}
        <div class="input">
            {{ form_errors(form.description) }}
            {{ form_widget(form.description, { 'attr': {'class': 'span10'}}) }}
        </div>
    </div>
    <div class="clearfix">
        {{ form_label(form.content) }}
        <div class="input">
            {{ form_errors(form.content) }}
            {{ form_widget(form.content, { 'attr': {'class': 'span10'}}) }}
        </div>
    </div>
    <input class="btn primary" type="submit" />
</form>
{% endblock %}